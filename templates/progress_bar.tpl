
<hr/>
<div class="progress">
  <div id="progress_bar" class="progress-bar" role="progressbar" aria-valuenow="{{stimuli_done + 1}}" aria-valuemin="1" aria-valuemax="{{stimuli_count}}" style="width: {{(100 * (stimuli_done + 1) / stimuli_count) }}%" >
           {{stimuli_done + 1}} / {{stimuli_count}}
  </div>
</div>

